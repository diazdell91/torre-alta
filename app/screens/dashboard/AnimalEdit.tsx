import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const AnimalEdit = (props: Props) => {
  return (
    <View>
      <Text>AnimalEdit</Text>
    </View>
  );
};

export default AnimalEdit;

const styles = StyleSheet.create({});
