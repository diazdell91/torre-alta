import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const CattleTree = (props: Props) => {
  return (
    <View>
      <Text>CattleTree</Text>
    </View>
  );
};

export default CattleTree;

const styles = StyleSheet.create({});
